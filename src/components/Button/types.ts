export type ButtonStyle = {
  bgcolor: string;
  borderRadius: string;
  color: string;
  pl: string;
  pr: string;
  fontSize: string;
};

export type ButtonType = {
  primary: ButtonStyle;
  secondary: ButtonStyle;
  text: ButtonStyle;
};
